/**
 * Created by edz on 2017/11/1.
 */

import CommonWidgetList from './commonWidgets';//封装通用控件样式、text、textarea、select、tree等
import CommonButtons from './commonButtons';//动态生成，通用样式按钮
import CommonFunc from './commonFunc';//通用函数
import CommonStyles from './commonStyle';//通用样式
import BaseCommonList from './baseCommonList';

module.exports = {
    CommonWidgetList: CommonWidgetList,
    CommonButtons: CommonButtons,
    CommonFunc: CommonFunc,
    CommonStyles: CommonStyles,
    BaseCommonList: BaseCommonList,
};